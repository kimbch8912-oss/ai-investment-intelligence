import { createServerSupabaseClient } from '../../supabase/server-client.ts';
export type RegisteredSource = { id: string; code: string };
export class SourceRegistrationRepository { private readonly db=createServerSupabaseClient(); async ensureTwelveData():Promise<RegisteredSource>{const existing=await this.db.from('system_sources').select('id,code').eq('code','TWELVE_DATA').maybeSingle();if(existing.error)throw Error(`SOURCE_READ: ${existing.error.message}`);if(existing.data)return existing.data;const inserted=await this.db.from('system_sources').insert({code:'TWELVE_DATA',name:'Twelve Data',source_type:'MARKET_DATA',category:'MARKET_DATA',country:'US',tier:'B',credential_env_key:'MARKET_DATA_API_KEY',metadata:{provider:'twelve-data'}}).select('id,code').single();if(inserted.error)throw Error(`SOURCE_INSERT: ${inserted.error.message}`);return inserted.data;} }
