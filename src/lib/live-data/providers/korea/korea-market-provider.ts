import type { MarketDataProvider, MarketDataRequest } from '../../provider-contracts.ts'; import type { MarketDataSnapshot, SourceEnvelope } from '../../types.ts'; import { mapKoreaDaily } from './mapper.ts'; import { validKoreaBars } from './validator.ts';
const now=()=>new Date().toISOString(); const fail=(code:string,message:string):SourceEnvelope<MarketDataSnapshot>=>({data:null,source:'twelve-data-korea',status:'FAILED',freshness:{retrievedAt:now(),dataAsOfTime:null,staleAfter:null,freshnessStatus:'UNKNOWN'},errors:[{source:'twelve-data-korea',code,message,retryable:code==='PROVIDER_NETWORK_ERROR'}]});
export class KoreaMarketDataProvider implements MarketDataProvider { readonly id='twelve-data-korea'; async getMarketData(request:MarketDataRequest):Promise<SourceEnvelope<MarketDataSnapshot>> { if(request.asset.country!=='KR'||request.asset.market!=='KRX')return fail('UNSUPPORTED_ASSET','Only KRX common stocks are supported.');const key=process.env.MARKET_DATA_API_KEY;if(!key)return fail('MISSING_API_KEY','MARKET_DATA_API_KEY is not configured.');try{const q=new URLSearchParams({symbol:request.asset.symbol,interval:'1day',outputsize:'250',end_date:request.asOfTime.slice(0,10),timezone:'UTC',adjust:'all'});const response=await fetch(`${process.env.MARKET_DATA_BASE_URL??'https://api.twelvedata.com'}/time_series?${q}`,{headers:{Authorization:`apikey ${key}`},cache:'force-cache',next:{revalidate:1800,tags:[`korea-market:${request.asset.symbol}`]}});const payload=await response.json();if(!response.ok)throw Error('HTTP');const data=mapKoreaDaily(payload,request.asOfTime);if(!validKoreaBars(data.ohlcv,request.asOfTime))return fail('INVALID_OHLCV','Korea OHLCV validation failed.');const asOf=data.ohlcv.at(-1)?.timestamp??null;return{data,source:'twelve-data-korea',status:data.ohlcv.length>=250?'READY':'PARTIAL',freshness:{retrievedAt:now(),dataAsOfTime:asOf,staleAfter:asOf?new Date(Date.parse(asOf)+48*3600000).toISOString():null,freshnessStatus:'UNKNOWN'},errors:data.ohlcv.length>=250?[]:[{source:'twelve-data-korea',code:'INSUFFICIENT_HISTORY',message:'Fewer than 250 daily bars were returned.',retryable:false}]};}catch{return fail('PROVIDER_NETWORK_ERROR','Korea market provider request failed.');} } }
