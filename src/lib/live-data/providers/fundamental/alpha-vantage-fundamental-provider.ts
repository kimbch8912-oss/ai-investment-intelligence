import type { FundamentalDataProvider, AssetDataRequest } from '../../provider-contracts.ts';
import type { FundamentalDataSnapshot, SourceEnvelope } from '../../types.ts';
import { mapAlphaVantageFundamental } from './mapper.ts';
const now = () => new Date().toISOString();
const failed = (code: string, message: string): SourceEnvelope<FundamentalDataSnapshot> => ({ data: null, source: 'alpha-vantage-fundamental', status: 'FAILED', freshness: { retrievedAt: now(), dataAsOfTime: null, staleAfter: null, freshnessStatus: 'UNKNOWN' }, errors: [{ source: 'alpha-vantage-fundamental', code, message, retryable: code === 'PROVIDER_NETWORK_ERROR' }] });
export class AlphaVantageFundamentalProvider implements FundamentalDataProvider { readonly id = 'alpha-vantage-fundamental'; async getFundamentalData(request: AssetDataRequest): Promise<SourceEnvelope<FundamentalDataSnapshot>> { if (!['NASDAQ', 'NYSE', 'NYSE American'].includes(request.asset.market) || request.asset.country !== 'US') return failed('UNSUPPORTED_ASSET', 'Only supported US equities can be loaded.'); const key = process.env.ALPHA_VANTAGE_API_KEY; if (!key) return failed('MISSING_API_KEY', 'ALPHA_VANTAGE_API_KEY is not configured.'); try { const base = process.env.ALPHA_VANTAGE_BASE_URL ?? 'https://www.alphavantage.co/query'; const call = async (fn: string) => { const response = await fetch(`${base}?${new URLSearchParams({ function: fn, symbol: request.asset.symbol, apikey: key })}`, { cache: 'force-cache', next: { revalidate: 43_200, tags: [`fundamental:${request.asset.symbol}:${fn}`] } }); const payload = await response.json(); if (!response.ok || payload.Note || payload.Information || payload['Error Message']) throw new Error('PROVIDER_RESPONSE_ERROR'); return payload; }; const [overview, income, balance, cash] = await Promise.all(['OVERVIEW', 'INCOME_STATEMENT', 'BALANCE_SHEET', 'CASH_FLOW'].map(call)); const retrievedAt = now(); const data = mapAlphaVantageFundamental(overview as Record<string, unknown>, income, balance, cash, retrievedAt, request.asset); const asOf = data.snapshot.asOfTime; if (asOf > request.asOfTime) return failed('LOOK_AHEAD_BLOCKED', 'Fundamental data is later than analysisTime.'); return { data, source: 'alpha-vantage-fundamental', status: data.snapshot.quality.status === 'COMPLETE' ? 'READY' : 'PARTIAL', freshness: { retrievedAt, dataAsOfTime: asOf, staleAfter: null, freshnessStatus: 'UNKNOWN' }, errors: [] }; } catch { return failed('PROVIDER_NETWORK_ERROR', 'Fundamental provider request or validation failed.'); } } }
