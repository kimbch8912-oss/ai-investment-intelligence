import { runM2Pipeline } from '../m2-pipeline.ts'; import type { EconomicPoint,MarketPricePoint } from '../../signals/types.ts';const ok=(v:unknown,m:string)=>{if(!v)throw new Error(m)};const market:Array<MarketPricePoint>=Array.from({length:100},(_,i)=>({assetId:'QA',marketTime:new Date(Date.UTC(2025,0,i+1)).toISOString(),close:String(100+i)}));const monthly:Array<EconomicPoint>=Array.from({length:14},(_,i)=>({seriesId:'CPI',observationDate:`${i<12?'2025':'2026'}-${String(i%12+1).padStart(2,'0')}-01`,value:String(100+i),vintageAt:'2026-03-01T00:00:00Z'}));const q=[['2025-01-01','100'],['2025-04-01','102'],['2025-07-01','105'],['2025-10-01','109'],['2026-01-01','114']].map(([observationDate,value])=>({seriesId:'GDP',observationDate,value,vintageAt:'2026-03-01T00:00:00Z'}));const liq=monthly.map(x=>({...x,seriesId:'M2',value:String(Number(x.value)*10)}));const a=runM2Pipeline(market,monthly,q,liq,'2026-03-02T00:00:00Z');const b=runM2Pipeline(market,monthly,q,liq,'2026-03-02T00:00:00Z');ok(a&&b,'pipeline');ok(JSON.stringify(a)===JSON.stringify(b),'determinism');ok(a!.calculations.market.return20d.status==='VALID'&&a!.signals.length===9&&a!.domainScores.length===7,'layers');ok(a!.configVersions.join(',')==='m2b-v1,m2c-v1,m2d-v1,m2e-v1,m2f-v1','versions');console.log('M2 completion E2E QA PASS');
