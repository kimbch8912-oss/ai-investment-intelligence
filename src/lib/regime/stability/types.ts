import type { Regime } from '../types.ts'; export interface StableRegimeInput{rawRegime:Regime;confidence:number;riskScore:number|null;asOfTime:string;previousStableRegime:Regime;previousStableSince:string;pendingCandidate?:Regime|null;pendingCount?:number} export interface StableRegimeResult{stableRegime:Regime;rawRegime:Regime;previousStableRegime:Regime;transitionStatus:'STABLE'|'PENDING_TRANSITION'|'TRANSITIONED'|'EMERGENCY_TRANSITION'|'INSUFFICIENT_EVIDENCE';transitioned:boolean;stableSince:string;asOfTime:string;confidence:number;pendingCandidate:Regime|null;pendingCount:number;requiredCount:number;adjustments:Array<{reasonCode:string;from:Regime;rawCandidate:Regime;to:Regime;pendingCount:number;requiredCount:number;riskScore:number|null;confidence:number}>;configVersion:string}
