import type { M2AnalysisSnapshot, MacroDirection } from '../macro/types.ts'; import type { AgentEvidence } from '../types.ts';
export interface KoreaMetric { status:'VALID'|'UNKNOWN'; direction:MacroDirection; strength:number|null; confidence:number; evidence:AgentEvidence[]; sourceStatus:'FIXTURE'|'UNAVAILABLE'; }
export interface KoreaMarketContext { asOfTime:string; kospi?:KoreaMetric; kosdaq?:KoreaMetric; usdKrw?:KoreaMetric; foreignFlow?:KoreaMetric; institutionalFlow?:KoreaMetric; semiconductor?:KoreaMetric; }
export interface KoreaMarketInput { m2:M2AnalysisSnapshot; korea?:KoreaMarketContext; }
export interface KoreaSourceSnapshot { marketScore:number|null; macroScore:number|null; riskScore:number|null; divergence:string; rawRegime:string; stableRegime:string; korea:Record<string,KoreaMetric|null>; coverage:number; }
export interface KoreaMarketOutput { agent:'korea_market'; analysisTime:string; dataAsOfTime:string|null; timeHorizon:'SHORT_TO_MEDIUM'; direction:MacroDirection; confidence:number; summary:string; positiveFactors:string[]; negativeFactors:string[]; risks:string[]; keyDrivers:string[]; marketStructure:{kospi:string;kosdaq:string;usdKrw:string;foreignFlow:string;institutionalFlow:string;semiconductor:string}; globalRelationship:{direction:string}; macroRelationship:{direction:string}; implications:string[]; evidence:AgentEvidence[]; unknowns:string[]; sourceSnapshot:KoreaSourceSnapshot; status:'VALID'|'PARTIAL'|'UNKNOWN'; }
