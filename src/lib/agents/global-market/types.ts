import type { M2AnalysisSnapshot } from '../macro/types.ts'; import type { MacroDirection } from '../macro/types.ts'; import type { AgentEvidence } from '../types.ts';
export type GlobalMarketInput = M2AnalysisSnapshot;
export interface GlobalMarketSourceSnapshot { marketScore:number|null; momentumScore:number|null; trendScore:number|null; riskScore:number|null; macroScore:number|null; marketRiskAdjustedScore:number|null; rawRegime:string; stableRegime:string; }
export interface GlobalMarketOutput { agent:'global_market'; analysisTime:string; dataAsOfTime:string|null; timeHorizon:'SHORT_TO_MEDIUM'; direction:MacroDirection; confidence:number; summary:string; positiveFactors:string[]; negativeFactors:string[]; risks:string[]; keyDrivers:string[]; marketStructure:{momentum:string;trend:string;risk:string;regime:string}; macroRelationship:{divergence:string;direction:string}; implications:string[]; evidence:AgentEvidence[]; unknowns:string[]; sourceSnapshot:GlobalMarketSourceSnapshot; status:'VALID'|'PARTIAL'|'UNKNOWN'; }
