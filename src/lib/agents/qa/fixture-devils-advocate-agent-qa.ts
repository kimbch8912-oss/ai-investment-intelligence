import{runDevilsAdvocateAgent}from'../devils-advocate/devils-advocate-agent.ts';import{DeterministicDevilFixtureClient}from'../devils-advocate/fixture-client.ts';import type{DevilFixtureClient,DevilInput,DevilOutput}from'../devils-advocate/types.ts';const at='2026-08-20T00:00:00Z',e={id:'e',type:'CALCULATION' as const,label:'e'},ok=(x:unknown,m:string)=>{if(!x)throw Error(m)},c=(ids=['e'],confidence=70,direction?:string)=>({id:'c',sourceAgent:'macro',claimType:'INFERENCE' as const,statement:'claim',direction,confidence,evidenceIds:ids,asOfTime:at}),i=(claims=[c()],m2?:DevilInput['m2Context']):DevilInput=>({claims,availableEvidence:[e],m2Context:m2,asOfTime:at});class M implements DevilFixtureClient{constructor(private f:(x:DevilOutput)=>DevilOutput){}generate(x:DevilInput){return this.f(new DeterministicDevilFixtureClient().generate(x))}}let r=runDevilsAdvocateAgent(i());ok(r.output?.challengeLevel==='LOW','well');r=runDevilsAdvocateAgent(i([c(['x'])]));ok(r.errors[0]?.code==='EVIDENCE_MISMATCH','unsupported');r=runDevilsAdvocateAgent(i([c()],{riskLevel:'HIGH'}));ok(r.output?.challengedClaims.length,'conflict');r=runDevilsAdvocateAgent(i([c(['e'],70,'BULL')],{stableRegime:'CAUTION'}));ok(r.output?.regimeContradictions.length,'regime');r=runDevilsAdvocateAgent(i([c([],90)]));ok(r.output?.overconfidenceFlags.length,'overconfidence');r=runDevilsAdvocateAgent(i([c()],undefined));ok(r.output?.evidenceGaps.length,'gap');r=runDevilsAdvocateAgent(i(),new M(x=>({...x,summary:'따라서 매도해야 한다'})));ok(r.errors[0]?.code==='AGENT_OUTPUT_INVALID','recommend');r=runDevilsAdvocateAgent(i(),new M(x=>({...x,summary:'곧 시장이 하락한다'})));ok(r.errors[0]?.code==='AGENT_OUTPUT_INVALID','prediction');r=runDevilsAdvocateAgent(i([]));ok(r.status==='UNKNOWN','none');const v=i(),before=JSON.stringify(v);r=runDevilsAdvocateAgent(v);ok(r.agent==='devils_advocate'&&r.agentVersion==='m3h-v1'&&r.promptVersion==='devils-advocate-agent-v1'&&JSON.stringify(v)===before,'envelope');ok(JSON.stringify(runDevilsAdvocateAgent(i()).output)===JSON.stringify(runDevilsAdvocateAgent(i()).output),'determinism');console.log('M3-H Devil runtime QA PASS');
