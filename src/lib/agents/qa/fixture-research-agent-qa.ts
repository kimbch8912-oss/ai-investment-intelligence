import{runResearchAgent}from'../research/research-agent.ts';import{DeterministicResearchFixtureClient}from'../research/fixture-client.ts';import type{ResearchDocument}from'../research/types.ts';const assert=(x:unknown,m:string)=>{if(!x)throw Error(m)};const doc=(method=true,findings:any[]=[{id:'f1',statement:'연관성이 관찰됐다.',findingType:'EMPIRICAL',direction:'POSITIVE'}]):ResearchDocument=>({id:'r1',title:'Research',sourceId:'s',sourceName:'Institute',sourceType:'ACADEMIC_PAPER',sourceTier:'A',authors:['A'],publishedAt:'2026-01-01T00:00:00Z',retrievedAt:'2026-01-01T00:00:00Z',researchQuestion:'Q',methodology:method?{design:'OBSERVATIONAL',methods:['panel'],sampleSize:100,markets:['US']}:undefined,dataset:{description:'data'},findings,limitations:[{id:'l1',statement:'sample limitation',type:'SAMPLE'}],affectedDomains:['RATES']});async function main(){const c=new DeterministicResearchFixtureClient();let r=await runResearchAgent(doc(),c);assert(r.status==='COMPLETED'&&r.output?.limitations.length,'strong');r=await runResearchAgent(doc(false),c);assert(r.output?.status==='PARTIAL','missing method');r=await runResearchAgent(doc(true,[{id:'p',statement:'p',findingType:'EMPIRICAL',direction:'POSITIVE'},{id:'n',statement:'n',findingType:'NULL_RESULT',direction:'MIXED'}]),c);assert(r.output?.direction==='NEUTRAL','mixed');r=await runResearchAgent(doc(true,[]),c);assert(r.errors[0]?.code==='AGENT_INPUT_INVALID','no finding');console.log('M3-E research agent QA PASS')}main();
