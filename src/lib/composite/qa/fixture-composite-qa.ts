import { buildCompositeSnapshot } from '../composite-snapshot.ts'; import type { DomainScore } from '../../scoring/types.ts';
const d=(domain:string,score:number|null,status:DomainScore['status']='VALID',confidence=1,asOfTime='2026-01-01T00:00:00Z'):DomainScore=>({domain,score,label:null,status,confidence,asOfTime,configVersion:'m2c-v1',components:[]});const ok=(v:unknown,m:string)=>{if(!v)throw new Error(m)};
function main(){const broad=[d('momentum',80),d('trend',80),d('risk',20),d('rates',70),d('inflation',70),d('growth',75),d('liquidity',70)];const a=buildCompositeSnapshot(broad);ok(a.market.score===80&&a.macro.score===71.5&&a.divergence.status==='ALIGNED','broad');const b=buildCompositeSnapshot([d('momentum',85),d('trend',80),d('risk',40),d('rates',30),d('inflation',35),d('growth',25),d('liquidity',35)]);ok(b.market.score!>b.macro.score!&&b.divergence.status==='DIVERGENCE'&&b.divergence.direction==='MARKET_ABOVE_MACRO','market macro weak');const c=buildCompositeSnapshot([d('momentum',30),d('trend',25),d('risk',70),d('rates',75),d('inflation',75),d('growth',80),d('liquidity',75)]);ok(c.macro.score!>c.market.score!&&c.divergence.direction==='MACRO_ABOVE_MARKET','macro market weak');const rally=buildCompositeSnapshot([d('momentum',80),d('trend',75),d('risk',90)]);ok(rally.marketRiskAdjustedScore!<rally.market.score!&&rally.market.components[2].rawScore===90&&rally.market.components[2].transformedScore===10,'risk transform');const partial=buildCompositeSnapshot([d('momentum',80),d('trend',null,'UNKNOWN',0),d('risk',20)]);ok(partial.market.status==='PARTIAL'&&partial.market.coverage===.6&&partial.market.score===80&&partial.market.confidence===.6,'partial');const insufficient=buildCompositeSnapshot([d('momentum',80),d('trend',null,'UNKNOWN',0),d('risk',null,'UNKNOWN',0)]);ok(insufficient.market.status==='UNKNOWN'&&insufficient.market.score===null&&insufficient.marketRiskAdjustedScore===null,'insufficient');ok(a.asOfTime==='2026-01-01T00:00:00Z'&&a.configVersion==='m2d-v1','time/config');console.log('M2-D composite QA PASS');} main();
