# M3-G Risk Agent QA Result

Fixture contract added. M2 Risk Level and confidence are immutable deterministic inputs; market/macro/fundamental/event/data risks are kept separate, with evidence allow-list and recommendation guard. No external calls, DB write, schema/RLS/seed change, or dependency change. All external-provider and deterministic-engine gaps remain pending.
